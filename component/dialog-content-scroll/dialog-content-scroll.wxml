<!--component/dialog-content-scroll/dialog-content-scroll.wxml-->
<dialog-bottom>
  <picker bindchange="bindPickerChange" value="{{index}}" range="{{array}}">
    <view class="picker">
      当前选择：{{array[index]}}
    </view>
  </picker>
</dialog-bottom>
